# Additional Python dependencies for enhanced LinkedIn Agent

# Existing (keep these)
groq>=0.9.0
requests>=2.31.0
cairosvg>=2.7.0
schedule>=1.2.0

# New dependencies for token refresh automation (optional but recommended)
pynacl>=1.5.0            # For encrypting GitHub secrets (token_manager.py)

# Note: All other functionality uses Python stdlib only
# If you skip pynacl, token auto-refresh will skip GitHub secret updates
# but token verification and alerts will still work
